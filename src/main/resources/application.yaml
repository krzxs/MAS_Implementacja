spring:
  application:
    name: wypozyczalnia
  datasource:
    url: jdbc:h2:file:./data/wypozyczalnia
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true

server:
  port: 8080